function adicionar() {
    let link = window.document.getElementById('imagem')
    let img = window.document.createElement('img')
    img.setAttribute('id', 'image')
    img.setAttribute('src', `${link}`)
    let p = 
    let res = window.document.getElementById('carta')
    res.appendChild(img)

}
    