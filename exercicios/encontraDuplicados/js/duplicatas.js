var lis = [2, 3, 5, 3, 8, 2, 9, 1, 8, 2]
var len = lis.length


for(i = 0; i < len; i++) {
    var cont = 0
    var num = lis[i]
    var pro = lis[cont]
    if (num === pro) {
        console.log(`${num} é igual a ${pro}`)
        
    } else {
        console.log(`${pro} é diferente de ${num}`)
    }
}

for(i = 0; i < len; i++) {
    var res = 0
    var cont = 1
    var num = lis[i]
    var pro = lis[cont]
    if (num === pro) {
        console.log(`${num} é igual a ${pro}`)
        
    } else {
        console.log(`${pro} é diferente de ${num}`)
    }
}

for(i = 0; i < len; i++) {
    var res = 0
    var cont = 2
    var num = lis[i]
    var pro = lis[cont]
    if (num === pro) {
        console.log(`${num} é igual a ${pro}`)
        
    } else {
        console.log(`${pro} é diferente de ${num}`)
    }
}